# Server Configuration
PORT=3000
REQUEST_TIMEOUT=30000
NODE_ENV=development

# Database Configuration
# Supported providers: sqlite (default), postgresql, mysql
DATABASE_PROVIDER=sqlite

# Database Connection URL
# SQLite: file:../database/analytics.db
# PostgreSQL: postgresql://user:password@localhost:5432/analytics?schema=public
# MySQL: mysql://user:password@localhost:3306/analytics
DATABASE_URL="file:../database/analytics.db"

# Analytics Dashboard Authentication
# Generate a secure token with: openssl rand -hex 32
# Minimum 32 characters required
# Default for development: dev-token-change-me-in-production-min32chars
# IMPORTANT: Change this in production!
ANALYTICS_API_TOKEN=dev-token-change-me-in-production-min32chars
